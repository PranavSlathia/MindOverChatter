[project]
name = "moc-tts"
version = "0.1.0"
description = "Text-to-speech service using Kokoro TTS"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]
